# E2E test overrides — applied on top of docker-compose.yml.
# Usage: docker compose -f docker-compose.yml -f docker-compose.test.yml ...
# See Makefile E2E_COMPOSE for the full invocation.

services:
  ib-gateway:
    restart: "no"

  remote-client:
    restart: "no"
    environment:
      API_TOKEN: test-token
      LISTENER_ENABLED: "true"
      LISTENER_EXEC_EVENTS_ENABLED: "true"
      LISTENER_EVENT_DEBOUNCE_TIME: "2000"
      NOTIFIERS: webhook
      WEBHOOK_SECRET: test-webhook-secret
      DEBUG_WEBHOOK_PATH: test-debug-path
      PYTHONPATH: /opt:/opt/poller-models
    ports:
      - "15010:5000"
    volumes:
      - ./services/remote-client:/app
      - ./services/notifier:/opt/notifier
      - ./services/dedup:/opt/dedup
      - ./services/shared:/opt/shared
      - ./services/poller/poller_models.py:/opt/poller-models/poller_models.py:ro
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:5000/health')",
        ]
      interval: 3s
      timeout: 5s
      retries: 20
      start_period: 10s

  poller:
    restart: "no"
    environment:
      IBKR_FLEX_TOKEN: test-flex-token
      IBKR_FLEX_QUERY_ID: "000000"
      API_TOKEN: test-token
      POLLER_API_PORT: "8000"
      DEBUG_WEBHOOK_PATH: test-debug-path
      PYTHONPATH: /opt
    ports:
      - "15011:8000"
    volumes:
      - ./services/poller:/app
      - ./services/notifier:/opt/notifier
      - ./services/dedup:/opt/dedup
      - ./services/shared:/opt/shared
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')",
        ]
      interval: 3s
      timeout: 5s
      retries: 20
      start_period: 5s

  ibkr-debug:
    restart: "no"
    deploy:
      replicas: 1
    environment:
      DEBUG_WEBHOOK_PATH: test-debug-path
    ports:
      - "15012:9000"
    volumes:
      - ./services/debug:/app
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:9000/health')",
        ]
      interval: 3s
      timeout: 5s
      retries: 10
      start_period: 3s

  # Disable production-only services
  novnc:
    profiles: ["disabled"]
  caddy:
    profiles: ["disabled"]
  gateway-controller:
    profiles: ["disabled"]
